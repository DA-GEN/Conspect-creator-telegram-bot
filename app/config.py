"""Конфигурация: переменные окружения, константы моделей/лимитов, промпты."""

import os
import re

from dotenv import load_dotenv

load_dotenv()

BOT_TOKEN = os.getenv("BOT_TOKEN")
GROQ_API_KEY = os.getenv("GROQ_API_KEY")

if not BOT_TOKEN:
    raise RuntimeError("BOT_TOKEN не найден. Заполните .env (см. .env.example).")
if not GROQ_API_KEY:
    raise RuntimeError("GROQ_API_KEY не найден. Заполните .env (см. .env.example).")

STT_MODEL = "whisper-large-v3"
LLM_MODEL_PRIMARY = "openai/gpt-oss-20b"
# Раньше тут стоял qwen/qwen3.8-27b — та же модель, что VISION_MODEL_PRIMARY.
# Из-за двойной нагрузки (и vision, и текстовый фолбэк) она выжигала свой
# дневной лимит токенов (TPD) Groq вдвое быстрее и роняла бота на весь день.
# gpt-oss-120b использует отдельную от vision-моделей квоту.
LLM_MODEL_FALLBACK = "openai/gpt-oss-120b"

# Vision-модели (принимают картинки) — нужны отдельно, т.к. LLM_MODEL_PRIMARY
# текстовая. qwen3.8 поддерживает до 3 картинок за запрос, qwen3.6 (fallback) —
# до 5, но у qwen3.6 на этом тарифе намного ниже лимит output-токенов в минуту
# (легко ловит 429), поэтому основной — qwen3.8. MAX_IMAGES — сколько картинок
# влезает в ОДИН запрос (берём по самой строгой модели); если картинок больше
# (например, в TikTok-слайдшоу) — analyze_images_sync бьёт их на батчи и
# анализирует все, а не только первые MAX_IMAGES.
VISION_MODEL_PRIMARY = "qwen/qwen3.8-27b"
VISION_MODEL_FALLBACK = "qwen/qwen3.6-27b"
MAX_IMAGES = 3
MAX_SLIDESHOW_IMAGES = 20  # разумный предел на случай очень длинных слайдшоу

MAX_AUDIO_BYTES = 25 * 1024 * 1024  # лимит free-тарифа Groq на размер аудиофайла
MAX_TRANSCRIPT_CHARS_FOR_LLM = 15000  # защита от переполнения контекстного окна модели

# Явные лимиты на длину ответа LLM: без них модель сама решает, сколько
# генерировать, и на free-тарифе Groq (низкий output-tokens-per-minute) это
# реально ловит 429 — уже наблюдали на qwen3.6-27b. Заодно ограничивает
# худший случай задержки. Модели gpt-oss (reasoning) тратят часть этого
# бюджета на скрытые reasoning-токены ДО видимого ответа — если бюджета не
# хватит, вернётся пустой ответ (см. groq_client.call_llm_with_fallback,
# который такое ловит и пробует следующую модель) — с запасом ниже это
# должно случаться реже.
MAX_TOKENS_VISION = 1000
MAX_TOKENS_SUMMARY = 2000
MAX_TOKENS_PROJECT_SUMMARY = 4000
MAX_TOKENS_CHAT = 1500

# Сколько батчей vision-анализа обрабатывать одновременно. ВАЖНО: на free-тарифе
# Groq лимит output-tokens-per-minute настолько мал (наблюдали 1000/мин на
# qwen3.6), что параллельные запросы бьют в ОДНУ и ту же квоту одновременно и
# только плодят 429 — эмпирически проверено: 2 параллельных батча дали 68с
# вместо ожидаемого ускорения (оба словили 429 и встали в очередь ретраев на
# десятки секунд). Поэтому — 1 (последовательно). Если апгрейднете тариф Groq
# (выше rate limit) — можно смело поднимать.
VISION_BATCH_CONCURRENCY = 1

# Видео в проекте — другое дело: между Groq-вызовами одного видео есть скачивание
# и ffmpeg, поэтому запросы разных видео естественно расходятся по времени и
# так не сталкиваются лбами так же сильно, как батчи одной vision-задачи.
PROJECT_CONCURRENCY = 3

CHAT_CONTEXT_TTL_SECONDS = 60 * 60  # результаты для кнопки чата не хранятся вечно
URL_CACHE_TTL_SECONDS = 30 * 60  # повторная присылка той же ссылки не гоняет пайплайн заново

URL_REGEX = re.compile(r"https?://[^\s]+", re.IGNORECASE)

# Спец-маркер: просим vision-модель вернуть ровно эту строку, если кадры не
# несут полезной информации (случайный/бессвязный видеоряд, говорящая голова
# и т.п.) — тогда визуальный раздел просто не добавляем в результат.
NO_VISUAL_INFO = "NO_VISUAL_INFO"

VISUAL_ANALYSIS_PROMPT = (
    "Ниже приложены {label} и расшифровка аудиодорожки (может быть пустой). "
    "Опиши кратко по пунктам, что важного показано на каждом изображении: "
    "ключевые сцены, весь текст на экране, детали, которых нет в расшифровке. "
    "Пиши только сами факты с картинок, без вступлений и общих выводов. "
    f"Если изображения не несут никакой полезной информации (случайный фон, "
    f"говорящая голова без контекста, бессвязный видеоряд и т.п.) — ответь "
    f"ровно одним словом: {NO_VISUAL_INFO}.\n\nРасшифровка аудио:\n{{transcript}}"
)

SUMMARY_PROMPT = (
    "Сделай подробный, структурированный конспект по материалу ниже — это "
    "расшифровка аудиодорожки видео и (если есть) описание визуального ряда. "
    "Выдели главные мысли, используй списки. Опирайся только на содержательную "
    "информацию: если расшифровка — это текст песни, музыка без слов или "
    "бессвязные звуки, а не осмысленная речь, не включай её в конспект и "
    "используй визуальные детали; если наоборот видеоряд не даёт полезной "
    "информации, опирайся только на речь. Если содержательной информации нет "
    "ни в речи, ни в видео — так и напиши.\n\nМатериал:\n{text}"
)

CONTEXT_QUERY_PROMPT = (
    "Пользователь прислал материал по видео (расшифровка речи и/или описание "
    "визуального ряда) и попросил конкретное: «{instruction}».\n"
    "Ответь ИМЕННО на этот запрос, используя материал ниже как источник. Не "
    "делай общий конспект и не добавляй разделы, которых пользователь не "
    "просил, — только то, что нужно по запросу, без лишнего. Если в материале "
    "нет нужной информации, так и скажи, не выдумывай.\n\nМатериал:\n{text}"
)
MAX_TOKENS_CONTEXT = 2000

MAX_PROJECT_VIDEOS = 20  # предел ссылок в одном проекте
MAX_CHARS_PER_VIDEO_IN_PROJECT = 4000  # чтобы много видео влезли в один запрос к LLM

PROJECT_SUMMARY_PROMPT = (
    "Ниже — материалы по нескольким видео: для каждого дана расшифровка речи "
    "и/или описание визуального ряда. Твоя задача:\n"
    "1. Определи тему и суть каждого видео.\n"
    "2. Сгруппируй видео по темам — видео на одну тему должны идти подряд, "
    "одним разделом с общим заголовком темы.\n"
    "3. Внутри каждого раздела структурированно распиши суть и ключевые детали "
    "каждого относящегося к теме видео (используй списки, заголовки).\n"
    "4. Не смешивай материал разных тем в одном разделе.\n"
    "5. Если по какому-то видео нет содержательной информации (ошибка "
    "скачивания или пустой материал) — коротко укажи это в подходящем месте, "
    "не выдумывай содержание.\n\nМатериалы:\n{materials}"
)

CHAT_SYSTEM_PROMPT = (
    "Ниже — расшифровка или конспект видео. Это основной источник для ответов "
    "на вопросы пользователя по теме видео. Но если пользователь спрашивает "
    "что-то, чего в этом тексте нет, просит подробнее раскрыть тему, сравнить "
    "с чем-то ещё или его не устраивает информация из текста — отвечай из "
    "своих общих знаний, не отказывайся и не ограничивайся только текстом. "
    "Не выдумывай факты о самом видео (кто их сказал, что именно происходило "
    "в кадре) — здесь опирайся строго на текст; но общие вопросы не по "
    "видео отвечай как обычный ассистент. Если отвечаешь не по тексту, а из "
    "общих знаний, коротко дай это понять.\n\nТекст:\n{content}"
)

MAX_CHAT_HISTORY_MESSAGES = 20  # ограничение, чтобы не раздувать контекст диалога

DOWNLOAD_USER_AGENT = (
    "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 "
    "(KHTML, like Gecko) Chrome/124.0.0.0 Safari/537.36"
)

# yt-dlp не умеет скачивать TikTok-слайдшоу (посты из нескольких фото,
# /photo/<id>) — при попытке падает с "Unsupported URL". Для таких ссылок
# используем tikwm.com как запасной способ (см. app.downloader).
TIKTOK_PHOTO_URL_REGEX = re.compile(r"tiktok\.com/[^\s]*?/photo/\d+", re.IGNORECASE)
TIKWM_API_URL = "https://www.tikwm.com/api/"
