# Токен от @BotFather в Telegram
BOT_TOKEN=

# Ключ с https://console.groq.com/keys (бесплатный тариф)
GROQ_API_KEY=

# Нужен только при деплое на Render (webhook-режим) — любая случайная строка,
# например `openssl rand -hex 32`. Локально и на VPS (polling) не используется.
WEBHOOK_SECRET=
